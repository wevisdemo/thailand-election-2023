/* @refresh reload */
import { customElement } from 'solid-element';
import Bottom from './bottom/bottom';
import Footer from './footer/footer';

customElement('election-bottom', Bottom);
customElement('election-footer', Footer);
